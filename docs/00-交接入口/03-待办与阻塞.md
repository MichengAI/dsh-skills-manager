# 待办与阻塞

更新时间：2026-08-16 17:05（Asia/Shanghai）

## 待办事项

### 上传弹窗自适应实机验收

- 负责人：YUJIYU
- 优先级：高
- 下一步：重新加载本地 `dsh-skills-manager` 后打开「上传插件」，确认投放区随设置页/窗口高度拉伸，按钮贴底且窄屏换行。
- 解除条件：大窗口无大块空白，缩小窗口后投放区和按钮仍完整可见。

### 本机安装与端到端验证

- 负责人：YUJIYU
- 优先级：高
- 下一步：安装 `@michengai/dsh-skills-manager@0.1.11` 并重启 DSH，验证上传、覆盖、启停、删除、公共 Agent 目录只读，以及中英文切换（设置 → 通用 → Language）。
- 解除条件：面板可操作，接口全链路通过，双语界面符合预期。

### 创建/编辑能力

- 负责人：YUJIYU
- 优先级：中
- 下一步：设计新建 skill 模板与在线编辑 frontmatter/正文的交互与接口。
- 解除条件：需求范围经确认后进入实施。

### 原生文件选择器与弹窗交互

- 负责人：YUJIYU
- 优先级：低
- 下一步：确认实际 DSH 桌面环境优先返回 `SKILL.md` 路径，或可经原生目录选择器返回插件目录；确认 ESC 只关闭最上层弹窗。
- 解除条件：两条上传路径至少一条在目标 DSH 版本可用，且嵌套弹窗关闭行为符合预期。

## 阻塞事项

无。
