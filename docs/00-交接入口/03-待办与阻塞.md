# 待办与阻塞

更新时间：2026-08-15 20:14（Asia/Shanghai）

## 待办事项

### npm 受信发布绑定

- 负责人：YUJIYU
- 优先级：高
- 下一步：在 npm 包设置的「Trusted Publisher」中绑定 GitHub Actions：用户 `MichengAI`、仓库 `dsh-skills-manager`、工作流 `publish.yml`，并允许 `npm publish`。
- 解除条件：推送一个新版本标签后，GitHub Actions 测试及 npm 发布均成功。

### 本机安装与端到端验证

- 负责人：YUJIYU
- 优先级：高
- 下一步：将当前仓库重新安装或重新加载到正在运行的 DSH Web profile 后重启 DSH，验证 DSH 上传、覆盖、启停、删除，以及公共 Agent 目录仅可查看。
- 解除条件：面板可操作，接口全链路通过。

### 创建/编辑能力

- 负责人：YUJIYU
- 优先级：中
- 下一步：设计新建 skill 模板与在线编辑 frontmatter/正文的交互与接口。
- 解除条件：需求范围经确认后进入实施。

### 原生文件选择器与弹窗交互

- 负责人：YUJIYU
- 优先级：低
- 下一步：确认实际 DSH 桌面环境优先返回 `SKILL.md` 路径，或可经原生目录选择器返回插件目录；确认 ESC 只关闭最上层弹窗。
- 解除条件：两条上传路径至少一条在目标 DSH 版本可用，且嵌套弹窗关闭行为符合预期。

## 阻塞事项

无。
