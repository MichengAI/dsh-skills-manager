# I022-上传弹窗自适应 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU / Codex
- 优先级：高
- 更新时间：2026-08-16 16:20（Asia/Shanghai）

## 目标与业务边界

修复上传弹窗被宿主设置页撑满后，投放区高度不跟随容器变化、下方大块空白的问题。不改变上传、覆盖确认、删除确认的业务逻辑，不发新版本。

## 任务清单

- [x] 上传弹窗增加 `dssm-modal-upload`，随遮罩/宿主对话框拉伸。
- [x] 虚线投放区 `flex: 1` 填充剩余高度，操作按钮贴底并允许换行。
- [x] 窄屏（`max-width: 560px`）下遮罩、弹窗和按钮改为铺满并换行。
- [x] 覆盖/删除确认框保持内容高度，不被上传框的拉伸规则影响。
- [x] 补充客户端 CSS 约束测试。

## 阻塞事项

无。

## 验收标准

- [x] `node --check lib\client.js` 通过。
- [x] `node test\core-test.mjs`：136 项通过。
- [x] `node test\locale-test.mjs`：449 项通过。
- [ ] 真实 DSH 客户端中打开上传弹窗，投放区随窗口/设置页高度变化，按钮不溢出。

## 已完成内容

上传弹窗改为跟随宿主可用区域自适应；投放区填充剩余空间，操作区贴底；窄屏可换行。

## 待处理事项

真实 DSH 客户端视觉验收转入交接入口待办。

## 关联提交

- `481d557` fix: 上传弹窗随宿主区域自适应

## 阅读顺序

1. 本文件
2. `docs\00-交接入口\02-当前状态.md`
3. `lib\client.js`
