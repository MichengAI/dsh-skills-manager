# I038-源码构建体系迁移

状态：已完成

负责人：YUJIYU

优先级：中

更新时间：2026-09-03 11:23（Asia/Shanghai）

## 目标与业务边界

将可维护源码与 npm 发布产物分离：源码固定在 `src\`，`lib\` 仅作为构建输出。不得改变 DSH 插件的包入口、Host 路由和浏览器 AMD 加载协议。

## 任务清单

- [x] 将 `core.js`、`index.js`、`client.js` 从 `lib\` 迁移到 `src\`。
- [x] 增加 `esbuild` 双端构建脚本，生成 Node ESM、浏览器 AMD 包与 source map。
- [x] 让测试先构建再验证 `lib\` 实际产物。
- [x] 发布工作流改为执行构建、测试、发布内容与产物一致性检查。
- [x] 更新开发、架构和交接文档。

## 阻塞事项

无。

## 验收标准

- `src\` 是唯一手工维护的运行源码目录。
- `lib\` 继续提供 `package.json` 既有入口及 DSH AMD 客户端契约。
- `npm test`、`npm run pack:check`、`git diff --check` 均通过。

## 已完成内容

- 构建通过 `esbuild@0.25.0` 从 `src\` 生成 `lib\core.js`、`lib\index.js`、`lib\client.js` 及对应 source map。
- 构建在同级暂存目录完成后才替换 `lib\`；受控失败测试确认旧产物保留且临时目录被清理。
- 新增构建契约测试，防止源码再次直接维护在 `lib\`。
- `npm run verify` 在发布前检查构建、测试、发布包及 `lib\` 产物同步（含未跟踪文件）。

## 待处理事项

后续 TypeScript 严格模式改造应另立迭代，避免将大型类型标注重构与本次路径/构建迁移耦合。

## 关联提交

待提交。

## 阅读顺序

1. `package.json`
2. `scripts\build.mjs`
3. `src\`
4. `test\build-test.mjs`
