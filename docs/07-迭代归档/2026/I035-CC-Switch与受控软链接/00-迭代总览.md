# I035 CC-Switch 与受控软链接

- 状态：已完成
- 负责人：YUJIYU / Codex
- 优先级：中
- 更新时间：2026-09-02 09:00（Asia/Shanghai）

## 目标与业务边界

新增固定 `~\.cc-switch\skills` 的只读、默认开启来源，并兼容 CC Switch 向 Codex、Claude 等用户级 Agent 根分发的顶层技能目录链接。不开放任意文件系统链接，不改变 DSH 可写来源、项目来源和导入的既有链接拒绝边界。

## 任务清单

- [x] 新增 `ccswitch` 用户来源，rank 510，支持来源级与逐 Skill 本地策略启停。
- [x] 兼容旧版 `state.json` 缺少 CC Switch 键的状态，默认补齐为开启。
- [x] 用户级只读来源允许目标为另一已知只读 Skills 根普通直接子目录的顶层 bundle 链接。
- [x] 按真实路径去重 CC Switch 分发副本，Provider 加载时复验真实 bundle 与文档路径。
- [x] 保持可写 DSH、项目来源、技能根链接、隐藏或非法目标、任意根外目标和导入链接拒绝。
- [x] 补齐中英文来源文案、README、领域文档与更新日志。
- [x] 在隔离 DSH Web `0.1.2-alpha.3` 中完成默认状态、旧状态迁移、两级启停、刷新持久化、junction 发现与去重回归。

## 阻塞事项

无。

## 验收标准

- [x] `~\.cc-switch\skills` 中的普通技能默认展示并由 `agent-ccswitch` Provider 加载。
- [x] 同一真实技能链接到 Codex 后不重复展示或注册候选。
- [x] 链接扫描后被改指时，旧 Provider 候选加载失败。
- [x] 旧状态自动得到 `sources.ccswitch=true` 和空策略列表，结构损坏状态仍失败关闭。
- [x] Windows junction 与普通符号链接走同一受控边界；无链接权限环境按既有测试约定跳过创建链接用例。
- [x] 核心、本地化、语法、打包和文档校验全部通过。
- [x] 真机开关前后两份 CC-Switch `SKILL.md` 的 SHA-256 保持不变，浏览器控制台与页面错误为空。

## 已完成内容

完成 CC Switch 固定来源、状态兼容、受控链接发现、真实路径去重、候选复验、客户端本地化和用户/架构文档更新。实现过程遵循 TDD，先得到 365 项通过、11 项预期失败，再完成最小实现和边界补测；发布前补充同一只读根内别名链接拒绝测试，最终核心测试 383 项、本地化测试 1468 项通过。

2026-09-02 使用临时用户目录、临时 `DSH_HOME` 和独立 Web profile 完成真实 DSH Web 回归。旧版状态在缺少 `ccswitch` 键时正常显示且默认开启；来源级和逐 Skill 启停均可恢复并跨刷新持久化；公共 Agent junction 被识别为只读 Skill，CC Switch 与 Codex 分发副本按真实路径只计一次。实时状态接口返回 2 个已启用 Skill、0 个诊断问题，测试前后源文件哈希一致，浏览器与服务日志未发现异常。

## 待处理事项

GitHub Issue #6 已发布[交付反馈](https://github.com/MichengAI/dsh-skills-manager/issues/6#issuecomment-5497183025)，并以 `COMPLETED` 关闭。隔离环境未配置有效工作区 Session，因此未补做 Session 作用域的官方 `skill.list` 调用；Provider 候选加载与链接改指复验继续由核心测试覆盖。

## 关联提交

- `5ad495c feat: add CC Switch skill source`
- `v0.1.33`

## 阅读顺序

1. 本文件。
2. `docs\00-交接入口\02-当前状态.md`。
3. `docs\00-交接入口\05-技术架构基线.md`。
4. `lib\core.js`。
5. `test\core-test.mjs`。
